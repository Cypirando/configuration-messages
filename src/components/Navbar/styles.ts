import styled from "styled-components";

export const StyledLogo = styled.div`
  height: 32px;
  margin: 16px;
  background: rgba(255, 255, 255, 0.2);
`;

export const StyledRoutesApp = styled.section`
  margin-top: 60px;
  display: flex;
  justify-content: center;
`;
